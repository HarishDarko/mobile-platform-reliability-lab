apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization
resources:
  - namespace.yaml
  - configmap.yaml
  - secret-example.yaml
  - deployment.yaml
  - service.yaml
# Ingress is optional because local clusters and GKE require an ingress controller or cloud load balancer setup.
# Uncomment after choosing an ingress path.
#  - ingress-example.yaml

